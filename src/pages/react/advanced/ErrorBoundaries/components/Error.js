import React from 'react';

export default function Errors(props) {
  const user = {
    name: 'name',
    gender: 'gender',
  };
  const x = user.girlFriend.join(',');
  return(
    <div>
      <p>{user.namexx}</p>
    </div>
  );
}