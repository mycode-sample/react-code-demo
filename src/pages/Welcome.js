import React from 'react';

export default function Welcome(props) {
  return(
    <div>
      {/* <h1 style={{fontSize: 72}}>welcome to react.</h1>
      {props.children} */}
      hello,welcome.
    </div>
  );
}