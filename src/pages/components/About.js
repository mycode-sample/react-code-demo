import React from 'react';
import { Nav } from '../Nav';

export default function About() {
  return(
    <div>
      <Nav/>
      <span style={{fontSize: "xxx-large"}}>About</span>
    </div>
  );
}
