import React from 'react';
import { Nav } from '../Nav';

export default function Users() {
  return(
    <div>
      <Nav/>
      <span style={{fontSize: "xxx-large"}}>Users</span>
    </div>
  );
}
